###########################################
# Carpihogar.ai - Variables de entorno
###########################################

# Base de datos (PostgreSQL)
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public

# NextAuth - claves y URL base
NEXTAUTH_SECRET=changeme_base64
# En desarrollo puede ser http://localhost:3000
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_URL="http://localhost:3000"

# (Opcional) Entorno y puerto
# NODE_ENV=development
# PORT=3000

# SMTP (para enviar recibos por email)
# SMTP_HOST=smtp.tu-proveedor.com
# SMTP_PORT=587
# SMTP_USER=usuario
# SMTP_PASS=contraseña
# SMTP_FROM="Carpihogar.ai <no-reply@tu-dominio.com>"

# Habilitar envíos de email (por defecto desactivado)
# EMAIL_ENABLED=false

# Verificación de correo obligatoria para iniciar sesión
# Desactivado por defecto para no bloquear usuarios existentes.
# ENFORCE_EMAIL_VERIFICATION=false

# OpenAI (para lectura de facturas con IA)
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# ManyChat (WhatsApp)
# MANYCHAT_API_KEY=your_manychat_api_key
# MANYCHAT_BASE_URL=https://api.manychat.com
# MANYCHAT_SEND_PATH=/whatsapp/sendMessage
# MANYCHAT_VERIFY_TOKEN=your_verify_token
# MANYCHAT_WEBHOOK_TOKEN=your_inbound_webhook_token

# Notas:
# - En producción, ajusta NEXTAUTH_URL / NEXT_PUBLIC_URL a tu dominio real
# - Asegúrate de establecer una clave aleatoria fuerte para NEXTAUTH_SECRET
# - Ejecuta: npx prisma generate && npx prisma migrate deploy
# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
