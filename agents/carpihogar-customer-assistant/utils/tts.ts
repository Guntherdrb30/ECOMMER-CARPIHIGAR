export async function generateAudio(text: string): Promise<string> {
  // Placeholder: integrate TTS provider and return base64 mp3
  // Return empty string to indicate no audio available
  return '';
}

